# /sessions — List active Claude Code sessions

Show all active Claude Code sessions registered with hello-claude.

## When to use

- When the user says `/sessions`
- When Claude needs to know what other sessions are running

## How to execute

Run the list-sessions script:

```bash
bash "$PLUGIN_DIR/scripts/list-sessions.sh"
```

Display the output to the user.
